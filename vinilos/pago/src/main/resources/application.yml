server:
  port: 8083

spring:
  application:
    name: vinilos3
  
  datasource:
    url: jdbc:mysql://localhost:3306/db_pagos
    username: root
    password: 
  
  flyway:
    enabled: true
    baseline-on-migrate: true
  
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect

# Rutas / servicios externos
vinilos1:
  url: http://localhost:8081/api/clientes
